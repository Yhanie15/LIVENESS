<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Lilo Admin - <%= title || 'Auth' %></title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
    <link rel="stylesheet" href="/css/main.css" />
    <% if (title === 'Login') { %>
      <link rel="stylesheet" href="/css/auth-login.css" />
    <% } else { %>
      <link rel="stylesheet" href="/css/auth-forgot-reset.css" />
    <% } %>
  </head>
  <body>
    <% if (title === 'Forgot Password' || title === 'Reset Password') { %>
      <div class="forgot-container">
    <% } else { %>
      <div class="login-container">
    <% } %>
      <div class="logo-section">
        <img src="/images/logo.png" alt="Lilo Logo" class="logo" />
        <div class="powered-by">
          <span>Powered by</span>
          <img src="/images/filmetrics.png" alt="Filmetrics Corporation" />
        </div>
      </div>
      <% if (title === 'Forgot Password') { %>
        <%- include('../auth/forgot-password.ejs') %>
      <% } else if (title === 'Reset Password') { %>
        <%- include('../auth/reset-password.ejs') %>
      <% } else { %>
        <%- include('../auth/login.ejs') %>
      <% } %>
    </div>
  </body>
</html>
